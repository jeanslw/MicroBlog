# 安全策略

## 支持的版本

由于本项目由个人维护，资源有限，安全更新将仅针对最新发布的主要版本。我们强烈建议所有用户始终升级到最新版本。

| 版本 | 支持情况 |
| ---- | -------- |
| v1.2.x | ✅ 当前支持，将接收安全更新 |
| v1.1.x 及更低版本 | ❌ 不再支持，请升级到最新版本 |

**说明**：仅最新稳定版会接收安全修复。建议订阅本仓库的 Release 通知，以便及时获取安全更新。

## 报告安全漏洞

我们非常重视安全问题。如果你发现了潜在的安全漏洞，请按照以下流程报告：

### 报告方式

1. **首选方式**：发送邮件至 **jeanslw@qq.com**，邮件标题请以 `[SECURITY]` 开头。
2. **备选方式**：在 GitHub Issues 中创建 Issue，但请**在标题中明确标注 `[SECURITY]`**，并**不要公开漏洞细节**，我们会先与你私下沟通。

### 报告内容

请在报告中尽可能包含以下信息：
- 受影响的版本号（如 v1.2.0）及 commit hash（如已知）
- 漏洞的简要描述
- 复现步骤（如果可能，请提供可复现的最小示例）
- 可能的攻击场景及影响范围
- 你的联系方式（如希望收到回复）

### 处理流程

1. **确认收到**：我们会在 48 小时内回复你的邮件，确认收到漏洞报告。
2. **评估与修复**：我们会尽快评估漏洞的严重性，并开始修复工作。目标是在 **7-14 天** 内发布补丁版本（具体时间取决于漏洞复杂程度）。
3. **公开披露**：修复完成后，我们会发布新版本，并在 Release Notes 中注明修复的安全问题。同时，我们会与报告者沟通，确认是否希望公开致谢。

### 漏洞处理原则

- 我们会以最严肃的态度对待每一个报告的安全问题。
- 在修复完成并发布新版本之前，我们不会公开披露漏洞细节。
- 如果漏洞被接受，你将在新版本发布后收到通知；如果被拒绝，我们将提供合理的解释。

### 关于依赖项的安全问题

如果你的漏洞报告涉及第三方依赖（如 Python 包），请同时向相关依赖的维护者报告。

## 常见安全实践建议

对于自部署的用户，我们建议：
- 始终使用最新版本
- 将管理后台（`/admin`）限制在内网或使用 VPN 访问
- 使用强密码设置 `BLOG_INIT_ADMIN_PWD`，并定期更换
- 设置强且唯一的 `BLOG_SECRET_KEY`（例如 `python -c 'import secrets;print(secrets.token_hex(32))'`）
- 生产环境启用 HTTPS，并开启安全 Session Cookie
- 定期审计日志，关注异常操作
- 在生产环境中使用 MySQL 而非 SQLite（SQLite 的并发写入能力有限）

---

感谢你帮助我们让 MicroBlog 变得更安全！
